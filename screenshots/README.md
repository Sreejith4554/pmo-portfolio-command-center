# Actual application captures

These PNGs were captured from the running local Python application with real Chromium during integration testing:

- `executive-portfolio.png`: baseline latest portfolio.
- `dependency-scenario.png`: edited P03 milestone showing 21-day downstream exposure.
- `project-detail.png`: River Recovery detail after reset.
- `mobile-portfolio.png`: 390-pixel-wide viewport with no page-level horizontal overflow.

They are evidence of this local tool, **not** a Power BI dashboard or a real employer implementation. The application and all displayed data are synthetic. Run the browser integration test to reproduce captures.
